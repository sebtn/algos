function reverseString(str) {
  var arrayString = str.split('').reverse().join('');
  //console.log(arrayString);
  //var reveredArray = arrayString.reverse();
  return arrayString;
}


reverseString("hello");
